[project]
name = "worker-metadata"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "shared",
    "ai",
]

[tool.uv.sources]
shared = { workspace = true }
ai = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/worker_metadata"]

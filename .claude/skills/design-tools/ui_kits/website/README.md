# Website UI kit

Marketing home page. `SiteHeader.jsx` (sticky, blurred), `Hero.jsx` (apricot wash + a live-looking answer preview built from the real `AnswerPanel`), `Sections.jsx` (three feature cards, a three-step strip on warm-50, a burgundy-gradient quote block, footer).

The only surface where `Button variant="accent"` and the burgundy gradient appear at full size. Copy follows the readme's content fundamentals: sentence case, no exclamation marks, disclaimer in the footer.

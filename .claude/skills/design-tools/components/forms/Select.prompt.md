Native dropdown styled to match Input; used for jurisdiction, court level, date range.

```jsx
<Select label="Jurisdiction" options={["All federal", "9th Circuit", "N.D. Cal."]} value={j} onChange={setJ} />
```

The signature Svk Beslutsök search box — wide, pill-cornered, one per screen.

```jsx
<SearchField value={q} onChange={setQ} onSubmit={run} scope="9th Cir. · 2015–present" />
```

Use full width on the home view; inside the workspace it sits in the header at 100% of the content column.

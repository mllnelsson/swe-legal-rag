Checkbox with optional description line; used for filter lists and export options.

```jsx
<Checkbox label="Only binding authority" description="Excludes out-of-circuit decisions" checked={v} onChange={setV} />
```

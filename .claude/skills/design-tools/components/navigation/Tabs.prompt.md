Section switcher. `underline` for page-level views, `pill` for panel-level switches.

```jsx
<Tabs tabs={[{value:"cases",label:"Cases",count:48},{value:"statutes",label:"Statutes",count:6}]} value={t} onChange={setT} />
```

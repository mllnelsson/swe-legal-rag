The container for everything. 12px radius, hairline border, whisper-soft shadow.

```jsx
<Card header="Recent matters" footer={<Button variant="ghost" size="sm">View all</Button>}>…</Card>
```

Tones: `default`, `accent` (apricot tint), `wash` (gradient), `inverse` (burgundy gradient, light text).

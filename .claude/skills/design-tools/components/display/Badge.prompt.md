Small status label. Read-only — never clickable (use Tag for removable filters).

```jsx
<Badge tone="binding">Binding</Badge>
<Badge tone="warn" icon={<Icon name="triangle-alert" size={12} />}>Criticized</Badge>
```
